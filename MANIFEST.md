# Monorepo Manifest

> **Auto-generated by `scripts/manifest/generate-manifest.ts`. Do not edit by hand — run the script after package changes.**

Packages: **3** across **1** categories. Tiers: universal: 3.

## Packages

| Package | Category | Tier | Version | Deps | Description |
|---|---|---|---|---:|---|
| `@repo/biome-config` | `tooling` | universal | 0.0.0 | 0 | This repo's own Biome rule overlay. Apps and packages extend both this and @sua… |
| `@repo/typescript-config` | `tooling` | universal | 0.0.0 | 0 | This repo's own tsconfig extension point, layered on top of @suaveplan/typescri… |
| `@repo/vite-config` | `tooling` | universal | 0.0.0 | 0 | This repo's own Vite library build config extension point, layered on top of @s… |

## Tier overlay

Tier is inferred per CLAUDE.md §16 priority order: browser → server → universal.

### universal (3)

- `@repo/biome-config` (`tooling`)
- `@repo/typescript-config` (`tooling`)
- `@repo/vite-config` (`tooling`)

## Dependency graph

Workspace edges only. Pipe to Graphviz: `awk '/^```dot/,/^```$/' MANIFEST.md | sed '1d;$d' | dot -Tsvg > graph.svg`.

```dot
digraph workspace {
  rankdir=LR;
  node [shape=box, fontname="monospace", fontsize=10];
}
```
